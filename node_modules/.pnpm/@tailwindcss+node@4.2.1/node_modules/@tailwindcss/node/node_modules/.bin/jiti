#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
else
  exec node  "$basedir/../../../../jiti/lib/jiti-cli.mjs" "$@"
fi
# cmd-shim-target=C:/Users/dropl/Documents/GitHub/Landing-hz/node_modules/.pnpm/@tailwindcss+node@4.2.1/node_modules/jiti/lib/jiti-cli.mjs
